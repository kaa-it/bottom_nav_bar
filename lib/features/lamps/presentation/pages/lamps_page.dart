import 'package:flutter/material.dart';

class LampsPage extends StatelessWidget {
  const LampsPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: RaisedButton(
          child: Text("Info"),
          onPressed: () {},
        ),
      ),
    );
  }
}
